const DestinationCards = ({ icelandCardImage }) => {
  return (
    <img
      className="self-stretch relative rounded-[8px] max-w-full overflow-hidden h-[317.05px] shrink-0 object-cover"
      alt=""
      src={icelandCardImage}
    />
  );
};

export default DestinationCards;
